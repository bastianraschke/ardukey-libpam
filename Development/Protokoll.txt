Protokoll:

Anfrage:
	[B]
	4	ID			Identifiziert, welcher Client authentifiziert werden soll
		OTP
		nonce		Gegen Replayattacken
		timeout?	Festlegen, wie lange OTP noch gültig ist, nachdem es auf dem Client erstellt wurde
		h			HMAC

Antwort:
	[B]
	4	ID
		OTP
		nonce
	2?	statuscode
				OK
				REPLAYED_OTP		Schon bekanntes OTP wieder eingespielt mit gültiger HMAC
				REPLAYED_REQUEST	genau der gleiche request wir nochmal eingespielt
				HMAC_FAIL			Signatur kaputt/ungültig
				NO_CLIENT_FOUND		Falsche ID angegeben
				OTP_BAD_FORMAT
				SERVER_ERROR
				OTHER??
		h

[B] Stellt die Länge in Byte dar.
4 Byte ergeben 32 Bit ~ 1 Milliarde mögliche Nutzer



otp revokable
device muss identity gebunden sein -> nicht einfach jedes device für "chef" accoutn verwendbar

- 

böse szenarien:

- Antwort des Auth servers muss absolut richtig sein > ELEMENTAR
  :gesendete nonce muss auch in der antwort iwie geprüft werden
- auth server dns spoofing:
  :nein, da kein shared secret
